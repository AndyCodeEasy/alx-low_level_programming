#include <stdio.h>
/**
 * main - Entry point
 *
 * description: prints all single digit numbers without printf
 *
 * Return: Always 0
 */

int main(void)
{
	int i;

	for (i = 48; i <= 57; i++)
	{
		putchar(i);
	}
	putchar('\n');

	return (0);
}
