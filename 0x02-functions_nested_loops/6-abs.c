#include "main.h"

/**
 * _abs - computes the absolute value of n
 * @n: the int to be checked
 * Return: the absolute value of int n
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
	return (0);
}
